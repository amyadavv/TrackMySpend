// Predefined category list matching backend validation
export const CATEGORIES = [
  'Food',
  'Utilities',
  'Entertainment',
  'Transport',
  'Housing',
  'Health',
  'Education',
  'Others'
];

// API base URL — set VITE_API_URL in frontend/.env
export const BASE_URL = import.meta.env.VITE_API_URL || 'http://localhost:5000';

